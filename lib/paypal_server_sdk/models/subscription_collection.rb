# paypal_server_sdk
#
# This file was automatically generated by APIMATIC v3.0
# ( https://www.apimatic.io ).

module PaypalServerSdk
  # The list of subscriptions.
  class SubscriptionCollection < BaseModel
    SKIP = Object.new
    private_constant :SKIP

    # An array of subscriptions.
    # @return [Array[Subscription]]
    attr_accessor :subscriptions

    # An array of request-related [HATEOAS
    # links](/docs/api/reference/api-responses/#hateoas-links).
    # @return [Array[LinkDescription]]
    attr_accessor :links

    # A mapping from model property names to API property names.
    def self.names
      @_hash = {} if @_hash.nil?
      @_hash['subscriptions'] = 'subscriptions'
      @_hash['links'] = 'links'
      @_hash
    end

    # An array for optional fields
    def self.optionals
      %w[
        subscriptions
        links
      ]
    end

    # An array for nullable fields
    def self.nullables
      []
    end

    def initialize(subscriptions: SKIP, links: SKIP)
      @subscriptions = subscriptions unless subscriptions == SKIP
      @links = links unless links == SKIP
    end

    # Creates an instance of the object from a hash.
    def self.from_hash(hash)
      return nil unless hash

      # Extract variables from the hash.
      # Parameter is an array, so we need to iterate through it
      subscriptions = nil
      unless hash['subscriptions'].nil?
        subscriptions = []
        hash['subscriptions'].each do |structure|
          subscriptions << (Subscription.from_hash(structure) if structure)
        end
      end

      subscriptions = SKIP unless hash.key?('subscriptions')
      # Parameter is an array, so we need to iterate through it
      links = nil
      unless hash['links'].nil?
        links = []
        hash['links'].each do |structure|
          links << (LinkDescription.from_hash(structure) if structure)
        end
      end

      links = SKIP unless hash.key?('links')

      # Create object from extracted values.
      SubscriptionCollection.new(subscriptions: subscriptions,
                                 links: links)
    end

    # Provides a human-readable string representation of the object.
    def to_s
      class_name = self.class.name.split('::').last
      "<#{class_name} subscriptions: #{@subscriptions}, links: #{@links}>"
    end

    # Provides a debugging-friendly string with detailed object information.
    def inspect
      class_name = self.class.name.split('::').last
      "<#{class_name} subscriptions: #{@subscriptions.inspect}, links: #{@links.inspect}>"
    end
  end
end
